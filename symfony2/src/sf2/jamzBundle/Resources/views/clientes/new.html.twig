{% extends '::base.html.twig' %}

{% block body -%}
    <h1>clientes creation</h1>

    <form action="{{ path('clientes_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('clientes') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
